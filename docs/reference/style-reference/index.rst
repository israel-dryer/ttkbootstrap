.. Generated by tools/generate_style_reference.py -- do not edit by hand.

Style Reference
===============

The deep hand-styling surface for the native ttk widgets ttkbootstrap themes.
For each widget family it documents the exact ``bootstyle`` -> ttk style-name
mapping, the element layout the widget is drawn from, the configurable options,
and the supported states -- everything you need to hand-tune a style with
``style.configure(...)`` and apply it with ``Widget(style=...)``.

.. note::

   These pages are **generated** from live ttk introspection by an offline
   ``tools/generate_style_reference.py`` step and committed as rST. Regenerate
   them after changing a widget recipe. This is the deep reference the
   :doc:`Widgets catalog </widgets/index>` links to for native widgets.

Styling ttk widgets
-------------------

.. grid:: 1 2 2 2
   :gutter: 3

   .. grid-item-card:: Button
      :link: button
      :link-type: doc

      A clickable action trigger.

   .. grid-item-card:: Checkbutton
      :link: checkbutton
      :link-type: doc

      A labeled on/off checkbox.

   .. grid-item-card:: Combobox
      :link: combobox
      :link-type: doc

      A text entry with a drop-down list.

   .. grid-item-card:: Entry
      :link: entry
      :link-type: doc

      A single-line text input.

   .. grid-item-card:: Frame
      :link: frame
      :link-type: doc

      A plain container surface.

   .. grid-item-card:: Label
      :link: label
      :link-type: doc

      A static text or image label.

   .. grid-item-card:: Labelframe
      :link: labelframe
      :link-type: doc

      A titled, bordered container.

   .. grid-item-card:: Menubutton
      :link: menubutton
      :link-type: doc

      A button that drops a menu.

   .. grid-item-card:: Notebook
      :link: notebook
      :link-type: doc

      A tabbed-pane container.

   .. grid-item-card:: Panedwindow
      :link: panedwindow
      :link-type: doc

      A split, draggable multi-pane container.

   .. grid-item-card:: Progressbar
      :link: progressbar
      :link-type: doc

      A determinate or indeterminate progress bar.

   .. grid-item-card:: Radiobutton
      :link: radiobutton
      :link-type: doc

      A labeled exclusive choice.

   .. grid-item-card:: Scale
      :link: scale
      :link-type: doc

      A draggable value slider.

   .. grid-item-card:: Scrollbar
      :link: scrollbar
      :link-type: doc

      A content scrollbar.

   .. grid-item-card:: Separator
      :link: separator
      :link-type: doc

      A thin dividing rule.

   .. grid-item-card:: Sizegrip
      :link: sizegrip
      :link-type: doc

      The resize handle in a window corner.

   .. grid-item-card:: Spinbox
      :link: spinbox
      :link-type: doc

      A numeric entry with step arrows.

   .. grid-item-card:: Treeview
      :link: treeview
      :link-type: doc

      A multi-column tree and table view.

Variant & shipped styles
------------------------

.. grid:: 1 2 2 2
   :gutter: 3

   .. grid-item-card:: Calendar
      :link: calendar
      :link-type: doc

      The date-picker calendar styling (used by DateEntry).

   .. grid-item-card:: Floodgauge
      :link: floodgauge
      :link-type: doc

      The ttk progressbar style behind the legacy Floodgauge.

   .. grid-item-card:: Toggle
      :link: toggle
      :link-type: doc

      A switch-style checkbutton (round or square).

   .. grid-item-card:: Toolbutton
      :link: toolbutton
      :link-type: doc

      A pressed/selected-state button for toolbars.

.. toctree::
   :hidden:
   :caption: Styling ttk widgets

   button
   checkbutton
   combobox
   entry
   frame
   label
   labelframe
   menubutton
   notebook
   panedwindow
   progressbar
   radiobutton
   scale
   scrollbar
   separator
   sizegrip
   spinbox
   treeview

.. toctree::
   :hidden:
   :caption: Variant & shipped styles

   calendar
   floodgauge
   toggle
   toolbutton
